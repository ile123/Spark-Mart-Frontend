type User = {
  id: string;
  wishlistId: string;
  firstName: string;
  lastName: string;
  phoneNumber: string;
  email: string;
  role: string;
  gender: string;
  addressId: string;
};

export type { User };
